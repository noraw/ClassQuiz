<!doctype html>

<html>
<head>
	<title>Welcome to QuizNow!</title>	
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link href="/css/bootstrap.min.css" rel="stylesheet">
	<link href="/css/bootstrap-theme.min.css" rel="stylesheet">
	<link href="/css/introHCI.css" rel="stylesheet">
	<link href='http://fonts.googleapis.com/css?family=Noto+Serif' rel='stylesheet' type='text/css'>
	<!-- this is a comment in HTML -->

    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
    <![endif]-->        
</head>

<body class="wholepage">
	<div class="container">
		<!-- h1 through h6 are headers. The higher the number, the smaller the header -->
		<!-- p means paragraph -->
		<div class="jumbotron">
			<div>
				<h1>Home page for {{userName}}</h1>
				<form class="navbar-form navbar-right" id="signOutForm" role="form" method="get" action="/user_logout">
					<input class="btn" type="submit" value="Sign Out"></input>
				</form>
			</div>
			
		</div>

		<form id="selectClassForm" role="form" method="get" action="/useExistingClass">
			<div class="form-group">
				<label for="classList">Select a current class:</label>
			    <select form="selectClassForm" class="form-control" id="classList" name="classList">
			      <option id="-" value='{"classID":0,"className":""}' name="-">-</option>
			    {{#each classes}}
			      <option id="{{_id}}" value='{"classID":"{{_id}}","className":"{{name}}" }' name="{{_id}}">{{name}}</option>
			    {{/each}}
			    </select>
			</div>
			<p>{{selectError}}</p>
			<input class="btn" type="submit" id="existingClassBtn" value="Use Existing Class"></input>
		</form>

		<form id="createClassForm" role="form" method="get" action="/createClass">
		    <div class="form-group">
			    <label for="newClassName">Create a new class:</label>
				<input type="text" class="form-control" placeHolder="New Class Name" id="newClassName" name="newClassName">
			</div>
			<p>{{createError}}</p>
			<input class="btn" type="submit" id="createClassBtn" value="Create New Class"></input>
		</form>


		<p>Remove a class:</p><br>
	    <select>
	    {{#each classes}}
	      <option value="{{class}}">{{class}}</option>
	    {{/each}}
	    </select>
	    
	    <button class="btn" type="button" onClick="removeClassBtn();">Remove Class</button><br><br><br>

		
	</div>
	

	<script src="https://code.jquery.com/jquery.js"></script>
	<script src="/js/bootstrap.min.js"></script>

</body>
</html>
